# Node backend (SIH/Vision/Backend). Defaults to http://localhost:5000/api when unset.
VITE_API_BASE_URL=http://localhost:5000/api

# The dev server runs on http://localhost:8080 — add that origin to the backend's
# CORS_ORIGIN (Backend/.env), e.g. CORS_ORIGIN=http://localhost:5173,http://localhost:8080
